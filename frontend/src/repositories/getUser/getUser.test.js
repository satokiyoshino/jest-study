import { getUser, _users } from "./index";

// jest.mock('./index');

describe('getUser', () => {
  test.todo('データが取れるかどうかのテスト')
  // test('データが取れるかどうかのテスト', async () => {
  //   const name = { name: Jon };
  //   const user = { "1": name };
  //   _users.get.mockResolvedValue(user);
  
  //   return getUser("1").then(data => expect(data).toEqual(name))
  // })
  
  test.todo('データが取れなかった時のテスト');
  
  test.todo('ユーザーがいなかった時のテスト');
})
